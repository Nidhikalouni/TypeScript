#  What are Generics in TypeScript? (Simple Explanation)

Generics ek aisa feature hai jisse hum function, class, ya interface banate waqt type fix nahi karte,
but when we use it, tab hum decide karte hain ki kaunsi type use hogi.
👉 Yaani type ko runtime par nahi,
but usage time par decide karne ka flexibility deti hai.

Generics allow you to create reusable components (functions, interfaces, classes)
that work with any type, while still maintaining type safety.

➡️ Instead of fixing the type when writing the function/class,
we specify the type when using it.
